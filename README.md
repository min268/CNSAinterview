# CNSAinterview
# 삼성고 면접 대비 프로젝트

생활기록부 PDF를 업로드하면 AI가 면접 질문을 자동생성한다.

## 시작하기

1. 패키지 설치
터미널을 열고 난 뒤에
npm install
를 터미널에 입력

2. 환경변수 설정
.env.local 파일 생성 후 파일 내부에
GEMINI_API_KEY=(여기에 만든 api키 입력)

3. 서버 실행
터미널을 열고 난 뒤에
npm run dev
를 터미널에 입력
